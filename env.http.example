# HTTP Service Configuration (Next.js Frontend)
# For Render deployment, this will use PORT=10000 by default

# Development
PORT=3002

# Production (Render will set PORT=10000 automatically)
NODE_ENV=production

# WebSocket Server URL (update with your WebSocket service URL)
NEXT_PUBLIC_WS_URL=ws://localhost:3003

# Production WebSocket URL (replace with your Render WebSocket service URL)
# NEXT_PUBLIC_WS_URL=wss://your-ws-service.onrender.com
